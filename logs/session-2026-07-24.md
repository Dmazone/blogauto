# 세션 로그 — 2026-07-24

## 오늘 작업 요약

### 1. trending-picks 신규 섹션 추가

**결정 사항:**
- 기존 12개 섹션에 트렌딩 비교·추천 상품 섹션 추가 → 총 13개
- B안 채택: us-trends 유지 + trending-picks 추가 → 하루 7개 포스팅
- group:0 (홀수/짝수 무관, 매일 실행)
- 쿠팡파트너스 trackingCode: AF8691300

**수정 파일:**
- `scripts/sections.js` — trending-picks 섹션 추가 (type:'product', group:0)
- `scripts/daily_runner.js` — group:0 필터 추가 (`dailySections`)
- `config.toml` — 메뉴에 "🛍️ 트렌드상품" 추가 (weight:110)
- `.github/workflows/scheduled-deploy.yml` — `0 23 * * *` 크론 추가 (08:00 KST)
- `content/posts/trending-picks/_index.md` — Hugo 섹션 페이지 생성

---

### 2. Gemini 이미지 생성 통합 (비용 절감)

**배경:**
- Gemini Pro 구독 기결제 (paydma@gmail.com)
- 구글 API 직접 호출 = 추가 비용 → Gemini 웹 UI Playwright 자동화 방식 채택

**구현 방식:**
- Gemini "이미지 만들기" 버튼 (입력창 "+" 메뉴) → Playwright로 클릭
- 생성된 이미지를 DOM에서 추출 (blob: URL → base64, 일반 URL → axios)
- sharp로 1280×720 webp 변환 → 저장
- 실패 시 Pollinations 폴백 유지

**생성 우선순위 변경:**
```
변경 전: Pollinations → NanoBanana → Flow 순
변경 후: Gemini 직접 생성 → Pollinations 폴백
```

**수정 파일:**
- `scripts/gemini_browser.js` — `useImageMaker()`, `extractImagesFromLastResponse()` 추가
- `scripts/agent_core.js` — `generateImagesViaGemini()`, `runProductPipeline()`, `runForSection()` 분기 추가

---

### 3. 이미지 품질 개선 (글 내용 연동)

**문제:** 기존 이미지 프롬프트가 섹션 스타일만 참조 → 글 내용과 무관한 이미지 생성

**해결:** finalBody에서 H2 섹션 제목 + 첫 문장 파싱 → 구체적 장면 요청
```javascript
const h2s = finalBody.match(/^## (.+)$/gm).map(h => h.replace(/^## /, '').trim());
// H2 제목 + 첫 문장 → Gemini에 [이미지 1] [이미지 2] [썸네일] 구분 요청
```

---

### 4. agent_core.js 주요 변경 사항

**runProductPipeline() — 6턴 상품 포스팅 파이프라인:**
- Turn 1: 트렌딩 상품 카테고리 실시간 검색
- Turn 2: 주제 확정 + 아웃라인 (JSON 파싱)
- Turn 3: 비교·추천 본문 + 쿠팡 링크
  `https://www.coupang.com/np/search?q=상품명&sourceType=affiliate&trackingCode=AF8691300`
- Turn 4: 자체검수 (글 품질 체크)
- Turn 5: 최종 마크다운 추출
- Turn 6: 이미지 프롬프트

**runForSection() 분기:**
```javascript
const result = section.type === 'product'
  ? await runProductPipeline(section, dateOverride)
  : await runWebPipeline(section, dateOverride);
```

---

### 5. CLAUDE.md 전면 재정리 (오늘 작업 완료)

**변경 요점:**
- 중복된 이미지 섹션 3개 → 1개 통합 ("이미지 기준")
- 이미지 생성 파이프라인 현행화 (Gemini 1차 → Pollinations 폴백)
- 일일 타임라인 업데이트 (6개 → 7개, 08:00까지)
- 트러블슈팅 섹션 신설 (이미지 누락·중복·발행 중단 복구 절차)
- "절대 금지" 테이블 정리 (개발 원칙과 중복 제거)
- trending-picks 규칙 명시 (쿠팡 링크 필수)

---

## 사용자 지시사항 (이 세션에서 확정)

- **존댓말 사용** — "존대해라" 지시 이후 적용
- **이미지 관련성은 내가(Claude) 핸들링** — "이건 네가 핸들링해야돼"
- **Gemini 잘 부려서 글 품질 계속 개선** — "글은 네가 제미나이 잘 부려서 더 나은 글 나오게 계속 학습시켜 너만믿는다"

---

## 현재 시스템 상태

| 항목 | 상태 |
|---|---|
| 섹션 수 | 13개 (trending-picks 추가) |
| 일일 포스팅 수 | 7개 |
| 이미지 생성 1차 | Gemini "이미지 만들기" 버튼 |
| 이미지 생성 폴백 | Pollinations flux 1280×720 |
| 발행 시간대 | KST 05:00~08:00 (30분 간격) |
| 쿠팡 trackingCode | AF8691300 |
| Gemini 계정 | paydma@gmail.com (DmA 01, Pro 구독) |
