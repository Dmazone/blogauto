/**
 * sns_promoter.js — SNS 자동 홍보 스텁
 * Threads 자동화 보류 중. promoteAll은 no-op으로 유지.
 * 재개 시: scripts/_archive/regen-misc/sns_promoter.js 참고
 */

export async function promoteAll() {
  // Threads 보류 중 — 아무 작업 안 함
}
